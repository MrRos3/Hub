r character: GetAttribute( "IsCarrying" ) then return false end if os.clock() - Runtime.LastCureAction < 5 then return false end return true end local function TryCureInfect() if not CanUseCureAction() then return false end local target = GetNearestCureSurvivor() if not target then return false end Runtime.LastCureAction = os.clock() pcall(function() CureInject: FireServer( target ) end) return true end local function TryCureCorpse() if not CanUseCureAction() then return false end local corpse = GetNearestCureCorpse() if not corpse then return false end Runtime.LastCureAction = os.clock() pcall(function() CureCorpse: FireServer( corpse ) end) return true end local function PrepareCureFlask() if not IsKiller() or os.clock() < Runtime.FlaskReadyAt then return false end pcall(function() CurePrepareFlask: FireServer() end) return true end local function CancelCureFlask() pcall(function() CureCancelFlask: FireServer() end) end local function ThrowCureFlask() if not IsKiller() or os.clock() < Runtime.FlaskReadyAt then return false end local camera = workspace.CurrentCamera if not camera then return false end Runtime.FlaskReadyAt = os.clock() + 15 pcall(function() CureThrowFlask: FireServer( camera.CFrame.LookVector, camera.CFrame.Position ) end) return true end local function HasParryDaggerEquipped() local character = LocalPlayer.Character if not character then return false end local tool = character: FindFirstChild( "Parrying Dagger" ) return tool and tool:IsA("Tool") end local function TryAutoParry() if not Settings.AutoParry or not IsSurvivor() or not HasParryDaggerEquipped() or os.clock() < Runtime.ParryReadyAt or IsBusy() then return false end local _, humanoid = GetCharacter() if not humanoid or humanoid.Health < humanoid.MaxHealth * 0.5 then return false end local character = LocalPlayer.Character if not character or CollectionService: HasTag( character, "Silenced" ) then return false end local _, _, myRoot = GetCharacter() if not myRoot then return false end local killerTeam = Teams: FindFirstChild( "Killer" ) local distance = math.huge if killerTeam then for _, player in ipairs( Players: GetPlayers() ) do if player.Team == killerTeam then local killerCharacter = player.Character local killerRoot = killerCharacter and killerCharacter: FindFirstChild( "HumanoidRootPart" ) local killerHumanoid = killerCharacter and killerCharacter: FindFirstChildOfClass( "Humanoid" ) if killerRoot and killerHumanoid and killerHumanoid.Health > 0 then distance = math.min( distance, ( killerRoot.Position - myRoot.Position ).Magnitude ) end end end end if distance > Settings.ParryDistance then return false end Runtime.LastParry = os.clock() Runtime.ParryReadyAt = os.clock() + 0.9 pcall(function() ParryEvent: FireServer() end) return true end Runtime: AddConnection( ParryResult .OnClientEvent: Connect( function( _, cooldown ) local seconds = tonumber( cooldown ) if seconds and seconds > 0 then Runtime.ParryReadyAt = os.clock() + seconds else Runtime.ParryReadyAt = os.clock() + 0.9 end end ) ) Runtime: AddConnection( UserInputService .InputBegan: Connect( function( input ) if input.UserInputType == Enum.UserInputType.MouseButton2 then Runtime.RMBHeld = true end end ) ) Runtime: AddConnection( UserInputService .InputEnded: Connect( function( input ) if input.UserInputType == Enum.UserInputType.MouseButton2 then Runtime.RMBHeld = false Runtime.AimTarget = nil end end ) )   Runtime: AddConnection( TwistVisualizeBullet .OnClientEvent: Connect( function( _, _, speed ) if type(speed) == "number" and speed >= 20 and speed <= 1500 then Runtime.LearnedProjectileSpeed = speed end end ) )    local function GetObjectPosition( object ) if not object then return nil end if object:IsA("BasePart") then return object.Position end if object:IsA("Model") then if object.PrimaryPart then return object .PrimaryPart .Position end local part = object: FindFirstChildWhichIsA( "BasePart", true ) if part then return part.Position end end return nil end local function GetAdorneePart( object ) if not object then return nil end if object:IsA("BasePart") then return object end if object:IsA("Model") then return object: FindFirstChild( "HumanoidRootPart" ) or object.PrimaryPart or object: FindFirstChildWhichIsA( "BasePart", true ) end return nil end local function GetVisibleHighlightTarget( object ) if not object then return nil end  if object:IsA("Model") then return object end   if object:IsA("BasePart") then local parent = object.Parent if parent and parent:IsA("Model") then return parent end local grandparent = parent and parent.Parent if grandparent and grandparent:IsA("Model") then return grandparent end end return object end local function DistanceFromPlayer( object ) local _, _, root = GetCharacter() local position = GetObjectPosition( object ) if not root or not position then return math.huge end return ( position - root.Position ).Magnitude end    local function FindNearestTagged( tags, maxDistance, predicate ) if type(tags) == "string" then tags = { tags } end local _, _, root = GetCharacter() if not root then return nil, math.huge end local best = nil local bestDistance = maxDistance or math.huge local seen = {} for _, tag in ipairs(tags) do local tagged = CollectionService: GetTagged(tag) for _, object in ipairs(tagged) do if not seen[object] and object: IsDescendantOf( workspace ) then seen[object] = true local position = GetObjectPosition( object ) if position then local distance = ( position - root.Position ).Magnitude if distance <= bestDistance then local valid = true if predicate then local ok, result = pcall( predicate, object ) valid = ok and result ~= false end if valid then best = object bestDistance = distance end end end end end end return best, bestDistance end    local function StopRepair( sendRemote ) local point = Runtime.CurrentRepair Runtime.CurrentRepair = nil if point and sendRemote ~= false and point.Parent then pcall(function() RepairEvent: FireServer( point, false ) end) end SetActionAttribute( "isRepairing", false ) local _, humanoid, root = GetCharacter() if root then root.Anchored = false end if humanoid then humanoid.AutoRotate = true end end local function StartRepair( point ) if Runtime.CurrentRepair or not point or not point.Parent or not IsSurvivor() then return false end local character, humanoid, root = GetCharacter() if not character or not humanoid or not root or humanoid.Health <= 50 then return false end if IsBusy() then return false end local generator = point.Parent local progress = tonumber( generator: GetAttribute( "RepairProgress" ) ) or 0 if progress >= 100 then return false end Runtime.CurrentRepair = point SetActionAttribute( "isRepairing", true ) humanoid.AutoRotate = false root.Anchored = true local generatorBody = generator: FindFirstChild( "GeneratorBody" ) or generator.PrimaryPart local targetPosition = generatorBody and GetObjectPosition( generatorBody ) or point.Position if targetPosition then local flatTarget = Vector3.new( targetPosition.X, root.Position.Y, targetPosition.Z ) if ( flatTarget - root.Position ).Magnitude > 0.01 then root.CFrame = CFrame.lookAt( root.Position, flatTarget ) end end pcall(function() RepairEvent: FireServer( point, true ) end) return true end local function TryRepairNearest() local point = FindNearestTagged( "GeneratorPoint", Settings.DetectionRadius, function(object) local generator = object.Parent if not generator then return false end local progress = tonumber( generator: GetAttribute( "RepairProgress" ) ) or 0 return progress < 100 and generator: GetAttribute( "Regressing" ) ~= true end ) if point then return StartRepair( point ) end return false end    local function StopHeal( sendRemote ) local target = Runtime.CurrentHeal Runtime.CurrentHeal = nil if target and sendRemote ~= false and target.Parent then pcall(function() HealEvent: FireServer( target, false ) end) end SetActionAttribute( "isHealing", false ) local _, humanoid = GetCharacter() if humanoid then humanoid.AutoRotate = true end end local function FindNearestInjuredSurvivor() local _, _, myRoot = GetCharacter() if not myRoot then return nil end local survivors = Teams: FindFirstChild( "Survivors" ) if not survivors then return nil end local best = nil local bestDistance = Settings.HealRadius for _, player in ipairs( Players: GetPlayers() ) do if player ~= LocalPlayer and player.Team == survivors then local character = player.Character local humanoid = character and character: FindFirstChildOfClass( "Humanoid" ) local root = character and character: FindFirstChild( "HumanoidRootPart" ) if humanoid and root and humanoid.Health > 0 and humanoid.Health < humanoid.MaxHealth and character: GetAttribute( "IsHooked" ) ~= true and character: GetAttribute( "IsCarried" ) ~= true then local distance = ( root.Position - myRoot.Position ).Magnitude if distance <= bestDistance then best = root bestDistance = distance end end end end return best, bestDistance end local function StartHeal( target ) if Runtime.CurrentHeal or not target or not target.Parent or not IsSurvivor() or IsBusy() then return false end Runtime.CurrentHeal = target SetActionAttribute( "isHealing", true ) local _, humanoid, root = GetCharacter() if humanoid and root then local position = GetObjectPosition( target ) if position then local flat = Vector3.new( position.X, root.Position.Y, position.Z ) if ( flat - root.Position ).Magnitude > 0.01 then root.CFrame = CFrame.lookAt( root.Position, flat ) end end humanoid.AutoRotate = false end pcall(function() HealEvent: FireServer( target, true ) end) return true end local function TryHealNearest() local target = FindNearestInjuredSurvivor() if target then return StartHeal( target ) end return false end    local function StopExit( sendRemote ) local point = Runtime.CurrentExit Runtime.CurrentExit = nil if point and sendRemote ~= false and point.Parent then pcall(function() LeverEvent: FireServer( point, false ) end) end SetActionAttribute( "isExiting", false ) end local function StartExit( point ) if Runtime.CurrentExit or not point or not point.Parent or not IsSurvivor() or IsBusy() then return false end local _, humanoid = GetCharacter() if not humanoid or humanoid.Health <= 50 then return false end Runtime.CurrentExit = point SetActionAttribute( "isExiting", true ) pcall(function() LeverEvent: FireServer( point, true ) end) return true end local function TryExitNearest() local point = FindNearestTagged( { "ExitPoint", "Exit", }, Settings.DetectionRadius ) if point then return StartExit( point ) end return false end    local function TryUnhookNearest() if not IsSurvivor() or IsBusy() then return false end if os.clock() - Runtime.LastUnhook < 2.1 then return false end local _, humanoid = GetCharacter() if not humanoid or humanoid.Health <= 50 then return false end local point = FindNearestTagged( "UnhookPoint", Settings.DetectionRadius ) if not point then return false end Runtime.LastUnhook = os.clock() SetActionAttribute( "isUnhooking", true ) pcall(function() UnHookEvent: FireServer( point ) end) task.delay( 2, function() if Runtime.Running then SetActionAttribute( "isUnhooking", false ) end end ) return true end    local function GetNearestKiller() local _, _, root = GetCharacter() if not root then return nil, math.huge end local best = nil local bestDistance = math.huge for _, killer in ipairs( CollectionService: GetTagged( "Killer" ) ) do if killer: IsDescendantOf( workspace ) then local position = GetObjectPosition( killer ) if position then local distance = ( position - root.Position ).Magnitude if distance < bestDistance then best = killer bestDistance = distance end end end end return best, bestDistance end    local function TryDropPallet( requireKiller ) if not IsSurvivor() or IsBusy() then return false end if os.clock() - Runtime.LastPallet < 1 then return false end if requireKiller then local _, killerDistance = GetNearestKiller() if killerDistance > Settings.PanicDistance then return false end end local point = FindNearestTagged( "PalletPoint", Settings.DetectionRadius ) if not point then return false end Runtime.LastPallet = os.clock() SetActionAttribute( "isDroppingPallet", true ) pcall(function() PalletDropEvent: FireServer( point ) end) task.delay( 1, function() if Runtime.Running then SetActionAttribute( "isDroppingPallet", false ) end end ) return true end    local function TryVaultNearest() if not IsSurvivor() or IsBusy() then return false end if os.clock() - Runtime.LastVault < 1 then return false end local character = LocalPlayer.Character local sprinting = character and character: GetAttribute( "Sprinting" ) == true local vault, vaultDistance = FindNearestTagged( "VaultPoint", Settings.DetectionRadius ) local slide, slideDistance = FindNearestTagged( "PalletPointSlide", Settings.DetectionRadius ) local point = nil local isSlide = false if vault and ( not slide or vaultDistance <= slideDistance ) then point = vault elseif slide then point = slide isSlide = true end if not point then return false end Runtime.LastVault = os.clock() if isSlide then SetActionAttribute( "isSliding", true ) pcall(function() PalletSlideEvent: FireServer( point, sprinting ) end) task.delay( 1.2, function() if Runtime.Running then SetActionAttribute( "isSliding", false ) end end ) else SetActionAttribute( "isVaulting", true ) pcall(function() VaultEvent: FireServer( point, sprinting ) end) task.delay( 1.2, function() if Runtime.Running then SetActionAttribute( "isVaulting", false ) end end ) end return true end    local function PressSpace()   if VirtualInputManager then local ok = pcall(function() VirtualInputManager: SendKeyEvent( true, Enum.KeyCode.Space, false, game ) task.wait(0.025) VirtualInputManager: SendKeyEvent( false, Enum.KeyCode.Space, false, game ) end) if ok then return true end end  if type(keypress) == "function" then local ok = pcall(function() keypress(0x20) task.wait(0.025) if type(keyrelease) == "function" then keyrelease( 0x20 ) end end) if ok then return true end end return false end local function GetSkillGui() local gui = LocalPlayer: FindFirstChild( "PlayerGui" ) if not gui then return nil end local skillGui = gui: FindFirstChild( "SkillCheckPromptGui" ) local check = skillGui and skillGui: FindFirstChild( "Check" ) if not check then return nil end local line = check: FindFirstChild( "Line" ) local goal = check: FindFirstChild( "Goal" ) return check, line, goal end local function DirectSkillSuccess( kind, args ) if kind == "Generator" then pcall(function() GeneratorSkillResult: FireServer( "success", 1, args[1], args[2] ) end) elseif kind == "Healing" then pcall(function() HealingSkillResult: FireServer( "success", 1, args[1] ) end) end end local function PerfectSkillCheck( kind, args, serial ) task.spawn(function()   task.wait(0.42) local deadline = os.clock() + 3 while Runtime.Running and os.clock() < deadline do if kind == "Generator" then if not Settings .PerfectGenerator or serial ~= Runtime .GeneratorSkillSerial then return end else if not Settings .PerfectHealing or serial ~= Runtime .HealingSkillSerial then return end end local check, line, goal = GetSkillGui() if check and line and goal then local rotation = tonumber( line.Rotation ) or 0 local goalRotation = tonumber( goal.Rotation ) or 0   local greatStart = 102 + goalRotation local greatEnd = 116 + goalRotation if rotation >= greatStart and rotation <= greatEnd then    if PressSpace() then return end   DirectSkillSuccess( kind, args ) return end end RunService .Heartbeat: Wait() end end) end    Runtime:AddConnection( GeneratorSkillCheck .OnClientEvent: Connect( function( checkId, repairPoint ) if not Settings .PerfectGenerator then return end local _, _, _, checkScript = GetCharacter() if checkScript and checkScript: GetAttribute( "isRepairing" ) ~= true then return end Runtime .Gener