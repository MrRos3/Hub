local ENV = getgenv and getgenv() or _G if ENV.__SALTY_SLASHER then pcall(function() ENV.__SALTY_SLASHER:Destroy() end) end    local Players = game:GetService("Players") local ReplicatedStorage = game:GetService("ReplicatedStorage") local Teams = game:GetService("Teams") local CollectionService = game:GetService("CollectionService") local RunService = game:GetService("RunService") local TweenService = game:GetService("TweenService") local CoreGui = game:GetService("CoreGui") local UserInputService = game:GetService("UserInputService") local Stats = game:GetService("Stats") local LocalPlayer = Players.LocalPlayer    local VirtualInputManager = nil pcall(function() VirtualInputManager = game:GetService( "VirtualInputManager" ) end)    local Runtime = { Running = true, Connections = {}, ESPRecords = {}, CurrentRepair = nil, CurrentHeal = nil, CurrentExit = nil, LastUnhook = 0, LastPallet = 0, LastVault = 0, LastSelfUnhook = 0, LastCureAction = 0, LastParry = 0, GeneratorSkillSerial = 0, HealingSkillSerial = 0, RMBHeld = false, AimTarget = nil, AimTargetPlayer = nil, AimVelocity = Vector3.new(), AimPredictedPosition = nil, LearnedProjectileSpeed = nil, ParryReadyAt = 0, FlaskReadyAt = 0, LocalEmoteTrack = nil, LocalEmoteSound = nil, CurrentEmoteName = nil, VantaUI = nil, Window = nil, } ENV.__SALTY_SLASHER = Runtime    local Settings = {  KillerESP = true, SurvivorESP = true, GeneratorESP = true, ExitESP = false, HookESP = false, PalletESP = false,  PerfectGenerator = true, PerfectHealing = true,  AutoRepair = false, AutoHeal = false, AutoUnhook = false, AutoExit = false, PanicPallet = false, AutoVault = false,  SpeedOverride = false, Speed = 22, AutoSprint = false, Crouch = false,  AutoSelfUnhook = false, SelfUnhookGuaranteedOnly = true,  AutoCureInfect = false, AutoCureCorpse = false, CureRange = 5,  HoldRMBAimbot = true, AimSmoothness = 0.32,  PredictMovingShots = true, PredictionStrength = 1.00, PredictionPingFactor = 0.70, PredictionMaxTime = 0.55, ProjectileSpeedFallback = 165, AutoParry = false, ParryDistance = 7,  DetectionRadius = 4,  HealRadius = 5,  PanicDistance = 11, }    function Runtime:AddConnection(connection) table.insert( self.Connections, connection ) return connection end    local Remotes = ReplicatedStorage: WaitForChild("Remotes") local GeneratorRemotes = Remotes: WaitForChild("Generator") local HealingRemotes = Remotes: WaitForChild("Healing") local CarryRemotes = Remotes: WaitForChild("Carry") local ExitRemotes = Remotes: WaitForChild("Exit") local PalletRemotes = Remotes: WaitForChild("Pallet") local WindowRemotes = Remotes: WaitForChild("Window") local MechanicsRemotes = Remotes: WaitForChild("Mechanics") local KillerRemotes = Remotes: WaitForChild("Killers") local CureRemotes = KillerRemotes: WaitForChild("Cure") local ItemsRemotes = Remotes: WaitForChild("Items") local ParryRemotes = ItemsRemotes: WaitForChild("Parrying Dagger") local TwistRemotes = ItemsRemotes: WaitForChild("Twist of Fate") local TwistVisualizeBullet = TwistRemotes: WaitForChild("VisualizeBullet") local ShopRemotes = Remotes: WaitForChild("Shop")  local RepairEvent = GeneratorRemotes: WaitForChild("RepairEvent") local GeneratorSkillCheck = GeneratorRemotes: WaitForChild("SkillCheckEvent") local GeneratorSkillResult = GeneratorRemotes: WaitForChild( "SkillCheckResultEvent" ) local RepairAnim = GeneratorRemotes: FindFirstChild("RepairAnim")  local HealEvent = HealingRemotes: WaitForChild("HealEvent") local HealingSkillCheck = HealingRemotes: WaitForChild("SkillCheckEvent") local HealingSkillResult = HealingRemotes: WaitForChild( "SkillCheckResultEvent" ) local HealAnim = HealingRemotes: FindFirstChild("HealAnim")  local UnHookEvent = CarryRemotes: WaitForChild("UnHookEvent")  local LeverEvent = ExitRemotes: WaitForChild("LeverEvent")  local PalletDropEvent = PalletRemotes: WaitForChild("PalletDropEvent") local PalletSlideEvent = PalletRemotes: WaitForChild("PalletSlideEvent")  local VaultEvent = WindowRemotes: WaitForChild("VaultEvent")  local SelfUnHookEvent = CarryRemotes: WaitForChild("SelfUnHookEvent")  local CureInject = CureRemotes: WaitForChild("inject") local CureCorpse = CureRemotes: WaitForChild("corpse") local CurePrepareFlask = CureRemotes: WaitForChild("PrepareFlask") local CureThrowFlask = CureRemotes: WaitForChild("ThrowFlask") local CureCancelFlask = CureRemotes: WaitForChild("CancelFlask")  local ChangeAttribute = MechanicsRemotes: WaitForChild("ChangeAttribute")  local ParryEvent = ParryRemotes: WaitForChild("parry") local ParryResult = ParryRemotes: WaitForChild("parryResult")  local EmoteHandler = Remotes: WaitForChild("EmoteHandler") local GetEquippedEmotes = ShopRemotes: WaitForChild("GetEquippedEmotes") local CheckEmoteOwnership = ShopRemotes: WaitForChild("CheckEmoteOwnership") local EmotesFolder = ReplicatedStorage: WaitForChild("Emotes")    pcall(function() local Modules = ReplicatedStorage: WaitForChild("Modules") local Survivors = Modules: WaitForChild("Survivors") local SurvivorConfig = require( Survivors: WaitForChild( "SurvivorConfig" ) ) if tonumber( SurvivorConfig.DETECTION_RADIUS ) then Settings.DetectionRadius = SurvivorConfig.DETECTION_RADIUS end if tonumber( SurvivorConfig.GENERATOR_DETECTION_RADIUS ) then Settings.DetectionRadius = SurvivorConfig .GENERATOR_DETECTION_RADIUS end Settings.HealRadius = ( tonumber( SurvivorConfig.DETECTION_RADIUS ) or 4 ) + 1 end)    local function Notify( title, content, icon ) print( "[Salty Slasher]", tostring(title), tostring(content or "") ) if not Runtime.VantaUI then return end pcall(function() Runtime.VantaUI: Notify({ Title = tostring(title), Content = tostring( content or "" ), Icon = icon or "info", Duration = 3, }) end) end    local function GetCharacter() local character = LocalPlayer.Character if not character then return end local humanoid = character: FindFirstChildOfClass( "Humanoid" ) local root = character: FindFirstChild( "HumanoidRootPart" ) local check = character: FindFirstChild( "CheckInterractable" ) return character, humanoid, root, check end local function IsSurvivor() local survivors = Teams: FindFirstChild( "Survivors" ) return survivors and LocalPlayer.Team == survivors end local function IsAlive() local _, humanoid = GetCharacter() return humanoid and humanoid.Health > 0 end local function IsBusy() local character, humanoid, root, check = GetCharacter() if not character or not humanoid or not root then return true end if character: GetAttribute( "MovementLocked" ) then return true end if character: GetAttribute( "IsHooked" ) then return true end if character: GetAttribute( "IsCarried" ) then return true end if CollectionService: HasTag( root, "doing action" ) then return true end if check then local attributes = { "isVaulting", "isSliding", "isDroppingPallet", "isRepairing", "isHealing", "isUnhooking", "isExiting", } for _, attribute in ipairs(attributes) do if check: GetAttribute( attribute ) then return true end end end return false end local function SetActionAttribute( name, value ) local _, _, _, check = GetCharacter() if check then pcall(function() check: SetAttribute( name, value ) end) end end    local function IsKiller() local killerTeam = Teams: FindFirstChild("Killer") return killerTeam and LocalPlayer.Team == killerTeam end local function IsGuaranteedSelfUnhook() local character = LocalPlayer.Character if not character then return false end return (character:GetAttribute("anticampCharge") or 0) >= 100 or (character:GetAttribute("karma") or 0) > 0 end local function TrySelfUnhook( ignoreGuarantee ) local character, humanoid = GetCharacter() if not character or not humanoid or humanoid.Health <= 0 or character:GetAttribute("IsHooked") ~= true or character:GetAttribute("IsCarried") == true or (character:GetAttribute("HookCount") or 0) >= 3 then return false end if not ignoreGuarantee and Settings.SelfUnhookGuaranteedOnly and not IsGuaranteedSelfUnhook() then return false end if os.clock() - Runtime.LastSelfUnhook < 3.05 then return false end Runtime.LastSelfUnhook = os.clock() pcall(function() SelfUnHookEvent: FireServer() end) return true end local function SetCrouch( value ) value = value == true Settings.Crouch = value local character = LocalPlayer.Character if not character then return end character: SetAttribute( "Crouching", value ) if value then character: SetAttribute( "Sprinting", false ) end pcall(function() ChangeAttribute: FireServer( "Crouchingserver", value ) end) end local function GetTeamTargets() local targets = {} local survivors = Teams: FindFirstChild("Survivors") local killer = Teams: FindFirstChild("Killer") for _, player in ipairs( Players: GetPlayers() ) do if player ~= LocalPlayer then local validTeam = false if LocalPlayer.Team == killer then validTeam = survivors and player.Team == survivors elseif LocalPlayer.Team == survivors then validTeam = killer and player.Team == killer end if validTeam then local character = player.Character local humanoid = character and character: FindFirstChildOfClass( "Humanoid" )   local root = character and ( character: FindFirstChild( "HumanoidRootPart" ) or character: FindFirstChild( "UpperTorso" ) or character: FindFirstChild( "Torso" ) or character: FindFirstChild( "Head" ) ) if humanoid and humanoid.Health > 0 and root then table.insert( targets, { Player = player, Character = character, Humanoid = humanoid, Part = root, } ) end end end end return targets end local function IsAimTargetVisible( entry ) local camera = workspace.CurrentCamera if not camera or not entry or not entry.Part or not entry.Character then return false end local origin = camera.CFrame.Position local direction = entry.Part.Position - origin local params = RaycastParams.new() params.FilterType = Enum.RaycastFilterType.Exclude params.IgnoreWater = true local ignore = {} if LocalPlayer.Character then table.insert( ignore, LocalPlayer.Character ) end params.FilterDescendantsInstances = ignore local hit = workspace: Raycast( origin, direction, params ) if not hit then return true end return hit.Instance and hit.Instance: IsDescendantOf( entry.Character ) end local function GetAimTarget() local camera = workspace.CurrentCamera if not camera then return nil end local viewport = camera.ViewportSize local center = Vector2.new( viewport.X * 0.5, viewport.Y * 0.5 ) local best = nil local bestScreenDistance = math.huge for _, entry in ipairs( GetTeamTargets() ) do local position, onScreen = camera: WorldToViewportPoint( entry.Part.Position ) if onScreen and position.Z > 0 and IsAimTargetVisible( entry ) then local distance = ( Vector2.new( position.X, position.Y ) - center ).Magnitude if distance < bestScreenDistance then bestScreenDistance = distance best = entry end end end return best end local function IsTwistOfFateEquipped() local character = LocalPlayer.Character if not character then return false end if character: FindFirstChild( "Twist of Fate" ) then return true end for _, descendant in ipairs( character: GetDescendants() ) do if descendant.Name == "Twist of Fate" then return true end end return false end local function GetNetworkPingSeconds() local fallback = 0.060 local ok, milliseconds = pcall(function() return Stats .Network .ServerStatsItem[ "Data Ping" ]: GetValue() end) if not ok or type(milliseconds) ~= "number" then return fallback end return math.clamp( milliseconds / 1000, 0, 0.30 ) end local function UpdateAimVelocity( entry, deltaTime ) if not entry or not entry.Character then Runtime.AimTargetPlayer = nil Runtime.AimVelocity = Vector3.new() return Vector3.new() end local root = entry.Character: FindFirstChild( "HumanoidRootPart" ) or entry.Part local measured = root and root.AssemblyLinearVelocity or Vector3.new() if Runtime.AimTargetPlayer ~= entry.Player then Runtime.AimTargetPlayer = entry.Player Runtime.AimVelocity = measured return measured end local alpha = 1 - math.exp( -18 * math.max( deltaTime or 0, 0 ) ) Runtime.AimVelocity = Runtime.AimVelocity: Lerp( measured, alpha ) return Runtime.AimVelocity end local function GetPredictedAimPosition( entry, deltaTime ) if not entry or not entry.Part or not entry.Part.Parent then return nil end local currentPosition = entry.Part.Position local velocity = UpdateAimVelocity( entry, deltaTime ) if not Settings.PredictMovingShots or not IsTwistOfFateEquipped() then Runtime.AimPredictedPosition = currentPosition return currentPosition end local camera = workspace.CurrentCamera if not camera then return currentPosition end local projectileSpeed = Runtime.LearnedProjectileSpeed or Settings.ProjectileSpeedFallback projectileSpeed = math.max( tonumber(projectileSpeed) or Settings.ProjectileSpeedFallback, 1 ) local distance = ( currentPosition - camera.CFrame.Position ).Magnitude  local travelTime = distance / projectileSpeed local pingLead = GetNetworkPingSeconds() * Settings.PredictionPingFactor local predictionTime = math.clamp( ( travelTime + pingLead ) * Settings.PredictionStrength, 0, Settings.PredictionMaxTime ) local predicted = currentPosition + velocity * predictionTime Runtime.AimPredictedPosition = predicted return predicted end local function GetNearestCureSurvivor() if not IsKiller() then return nil, math.huge end local _, _, root = GetCharacter() if not root then return nil, math.huge end local survivors = Teams: FindFirstChild("Survivors") if not survivors then return nil, math.huge end local best = nil local bestDistance = Settings.CureRange for _, player in ipairs( Players: GetPlayers() ) do if player.Team == survivors then local character = player.Character local humanoid = character and character: FindFirstChildOfClass( "Humanoid" ) local targetRoot = character and character: FindFirstChild( "HumanoidRootPart" ) if character and humanoid and humanoid.Health > 0 and targetRoot and character: GetAttribute( "IsHooked" ) ~= true and character: GetAttribute( "IsCarried" ) ~= true and ( character: GetAttribute( "Cured" ) or 0 ) <= ( character: GetAttribute( "HookCount" ) or 0 ) then local distance = ( targetRoot.Position - root.Position ).Magnitude if distance <= bestDistance then best = character bestDistance = distance end end end end return best, bestDistance end local function CountActiveCureCorpses() local count = 0 for _, model in ipairs( CollectionService: GetTagged("049-2") ) do if model:IsA("Model") and model: IsDescendantOf( workspace ) and model: GetAttribute( "CorpseCreated0492" ) == true then local humanoid = model: FindFirstChildOfClass( "Humanoid" ) if not humanoid or humanoid.Health > 0 then count = count + 1 end end end return count end local function GetNearestCureCorpse() if not IsKiller() or CountActiveCureCorpses() >= 4 then return nil, math.huge end local character, _, root = GetCharacter() if not character or not root or ( character: GetAttribute( "corpsecharge" ) or 0 ) <= 0 then return nil, math.huge end local best = nil local bestDistance = Settings.CureRange local rayParams = RaycastParams.new() rayParams.FilterType = Enum.RaycastFilterType.Exclude rayParams.FilterDescendantsInstances = {character} for _, corpse in ipairs( CollectionService: GetTagged("corpse") ) do local corpseRoot = corpse:IsA("Model") and corpse: FindFirstChild( "HumanoidRootPart" ) if corpseRoot then local delta = corpseRoot.Position - root.Position local distance = delta.Magnitude if distance <= bestDistance then local ray = workspace: Raycast( root.Position, delta, rayParams ) if ray and ray.Instance and ray.Instance: IsDescendantOf( corpse ) then best = corpse bestDistance = distance end end end end return best, bestDistance end local function CanUseCureAction() if not IsKiller() then return false end local character, humanoid = GetCharacter() if not character or not humanoid or humanoid.Health <= 0 or character: GetAttribute( "Immobile" ) or character: GetAttribute( "IsStunned" ) o