atorSkillSerial = Runtime .GeneratorSkillSerial + 1 PerfectSkillCheck( "Generator", { checkId, repairPoint, }, Runtime .GeneratorSkillSerial ) end ) )    Runtime:AddConnection( HealingSkillCheck .OnClientEvent: Connect( function( checkId ) if not Settings .PerfectHealing then return end local _, _, _, checkScript = GetCharacter() if checkScript and checkScript: GetAttribute( "isHealing" ) ~= true then return end Runtime .HealingSkillSerial = Runtime .HealingSkillSerial + 1 PerfectSkillCheck( "Healing", { checkId, }, Runtime .HealingSkillSerial ) end ) )    if RepairAnim then Runtime:AddConnection( RepairAnim .OnClientEvent: Connect( function( _, _, _, _, finished ) if finished then StopRepair( false ) end end ) ) end if HealAnim then Runtime:AddConnection( HealAnim .OnClientEvent: Connect( function(active) if active == false then StopHeal( false ) end end ) ) end    local ESPFolder = Instance.new("Folder") ESPFolder.Name = "SaltySlasherESP" local espParent = CoreGui if type(gethui) == "function" then local ok, result = pcall( gethui ) if ok and result then espParent = result end end ESPFolder.Parent = espParent Runtime.ESPFolder = ESPFolder    local function DestroyESPRecord( object ) local record = Runtime .ESPRecords[ object ] if not record then return end if record.Highlight then pcall(function() record.Highlight: Destroy() end) end if record.Billboard then pcall(function() record.Billboard: Destroy() end) end Runtime .ESPRecords[ object ] = nil end local function EnsureESP( object, adornee, label, color, seen, highlightTarget ) if not object or not adornee or not adornee.Parent then return end seen[object] = true highlightTarget = highlightTarget or object local record = Runtime .ESPRecords[ object ] if not record then record = {} local highlight = Instance.new( "Highlight" ) highlight.Name = "SaltyHighlight" highlight.DepthMode = Enum .HighlightDepthMode .AlwaysOnTop highlight.FillTransparency = 0.78 highlight.OutlineTransparency = 0.05 highlight.Adornee = highlightTarget highlight.Parent = ESPFolder record.Highlight = highlight local billboard = Instance.new( "BillboardGui" ) billboard.Name = "SaltyLabel" billboard.AlwaysOnTop = true billboard.Size = UDim2.new( 0, 220, 0, 36 ) billboard.StudsOffset = Vector3.new( 0, 3, 0 ) billboard.Adornee = adornee billboard.Parent = ESPFolder local text = Instance.new( "TextLabel" ) text.Name = "Label" text.BackgroundTransparency = 1 text.Size = UDim2.fromScale( 1, 1 ) text.Font = Enum.Font.GothamBold text.TextSize = 14 text.TextStrokeTransparency = 0.25 text.Parent = billboard record.Billboard = billboard record.Text = text Runtime .ESPRecords[ object ] = record end if record.Highlight then record.Highlight.Adornee = highlightTarget record.Highlight .FillColor = color record.Highlight .OutlineColor = color end if record.Billboard then record.Billboard.Adornee = adornee end if record.Text then record.Text.Text = label record.Text.TextColor3 = color end end    local function RefreshESP() local seen = {}    if Settings.KillerESP then local killers = CollectionService: GetTagged( "Killer" ) for _, killer in ipairs(killers) do if killer: IsDescendantOf( workspace ) then local part = GetAdorneePart( killer ) if part then local player = Players: GetPlayerFromCharacter( killer ) local name = player and player.Name or killer.Name local distance = math.floor( DistanceFromPlayer( killer ) + 0.5 ) EnsureESP( killer, part, "KILLER • " .. name .. " • " .. tostring( distance ) .. "m", Color3.fromRGB( 255, 75, 75 ), seen ) end end end end    if Settings.SurvivorESP then local survivors = Teams: FindFirstChild( "Survivors" ) if survivors then for _, player in ipairs( Players: GetPlayers() ) do if player ~= LocalPlayer and player.Team == survivors then local character = player.Character local humanoid = character and character: FindFirstChildOfClass( "Humanoid" ) local root = character and character: FindFirstChild( "HumanoidRootPart" ) if character and humanoid and root and humanoid.Health > 0 then local distance = math.floor( ( root.Position - ( select( 3, GetCharacter() ) and select( 3, GetCharacter() ).Position or root.Position ) ).Magnitude + 0.5 ) local health = math.floor( humanoid.Health + 0.5 ) EnsureESP( character, root, player.Name .. " • " .. tostring( health ) .. " HP • " .. tostring( distance ) .. "m", Color3.fromRGB( 85, 210, 255 ), seen ) end end end end end    if Settings.GeneratorESP then local generators = CollectionService: GetTagged( "Generator" ) for _, generator in ipairs(generators) do if generator: IsDescendantOf( workspace ) then local progress = tonumber( generator: GetAttribute( "RepairProgress" ) ) or 0 if progress < 100 then local part = GetAdorneePart( generator ) if part then local distance = math.floor( DistanceFromPlayer( generator ) + 0.5 ) EnsureESP( generator, part, "GENERATOR • " .. tostring( math.floor( progress + 0.5 ) ) .. "% • " .. tostring( distance ) .. "m", Color3.fromRGB( 255, 205, 75 ), seen ) end end end end end    if Settings.ExitESP then for _, exit in ipairs( CollectionService: GetTagged( "Exit" ) ) do local part = GetAdorneePart( exit ) if part and exit: IsDescendantOf( workspace ) then EnsureESP( exit, part, "EXIT • " .. tostring( math.floor( DistanceFromPlayer( exit ) + 0.5 ) ) .. "m", Color3.fromRGB( 90, 255, 120 ), seen, GetVisibleHighlightTarget( exit ) ) end end end    if Settings.HookESP then   for _, hook in ipairs( CollectionService: GetTagged( "Spike" ) ) do local part = GetAdorneePart( hook ) if part and hook: IsDescendantOf( workspace ) then EnsureESP( hook, part, "HOOK", Color3.fromRGB( 220, 100, 255 ), seen, hook ) end end end    if Settings.PalletESP then   for _, pallet in ipairs( CollectionService: GetTagged( "pallet" ) ) do local part = GetAdorneePart( pallet ) if part and pallet: IsDescendantOf( workspace ) then EnsureESP( pallet, part, "PALLET", Color3.fromRGB( 255, 150, 70 ), seen, pallet ) end end end    local stale = {} for object in pairs( Runtime.ESPRecords ) do if not seen[object] then table.insert( stale, object ) end end for _, object in ipairs(stale) do DestroyESPRecord( object ) end end    task.spawn(function() while Runtime.Running do pcall( RefreshESP ) task.wait(0.35) end end)    task.spawn(function() while Runtime.Running do    if Runtime.CurrentRepair then local point = Runtime.CurrentRepair local generator = point and point.Parent local progress = generator and tonumber( generator: GetAttribute( "RepairProgress" ) ) or 100 local _, humanoid = GetCharacter() if not Settings.AutoRepair or not point.Parent or progress >= 100 or not humanoid or humanoid.Health <= 50 then StopRepair( progress < 100 ) end elseif Settings.AutoRepair and IsSurvivor() and not IsBusy() then TryRepairNearest() end    if Runtime.CurrentHeal then local target = Runtime.CurrentHeal local character = target and target.Parent local humanoid = character and character: FindFirstChildOfClass( "Humanoid" ) local distance = target and DistanceFromPlayer( target ) or math.huge if not Settings.AutoHeal or not target.Parent or not humanoid or humanoid.Health >= humanoid.MaxHealth or distance > Settings.HealRadius + 1 then StopHeal( humanoid and humanoid.Health < humanoid.MaxHealth ) end elseif Settings.AutoHeal and IsSurvivor() and not IsBusy() then TryHealNearest() end    if Runtime.CurrentExit then if not Settings.AutoExit or not Runtime .CurrentExit .Parent or DistanceFromPlayer( Runtime.CurrentExit ) > Settings.DetectionRadius + 1 then StopExit( true ) end elseif Settings.AutoExit and IsSurvivor() and not IsBusy() then TryExitNearest() end    if Settings.AutoUnhook and IsSurvivor() and not IsBusy() then TryUnhookNearest() end    if Settings.PanicPallet and IsSurvivor() and not IsBusy() then TryDropPallet( true ) end    if Settings.AutoVault and IsSurvivor() and not IsBusy() then local character = LocalPlayer.Character if character and character: GetAttribute( "Sprinting" ) == true then TryVaultNearest() end end    if Settings.AutoSelfUnhook and IsSurvivor() then TrySelfUnhook( false ) end    if Settings.AutoCureInfect then TryCureInfect() end if Settings.AutoCureCorpse then TryCureCorpse() end    TryAutoParry()    if Settings.AutoSprint then local character, humanoid = GetCharacter() if character and humanoid and humanoid.Health > 0 then local shouldSprint = humanoid.MoveDirection.Magnitude > 0.05 and character: GetAttribute( "Crouching" ) ~= true and not IsBusy() character: SetAttribute( "Sprinting", shouldSprint ) end end task.wait(0.10) end end)    Runtime:AddConnection( RunService .RenderStepped: Connect( function() if not Runtime.Running or not Settings .SpeedOverride then return end local character, humanoid = GetCharacter() if not character or not humanoid or humanoid.Health <= 0 then return end if character: GetAttribute( "MovementLocked" ) or character: GetAttribute( "IsHooked" ) or character: GetAttribute( "IsCarried" ) then return end if character: GetAttribute( "Crouching" ) then return end if IsBusy() then return end humanoid.WalkSpeed = Settings.Speed end ) )            Runtime: AddConnection( RunService .RenderStepped: Connect( function(deltaTime) if not Runtime.Running or not Settings .HoldRMBAimbot or not Runtime.RMBHeld then Runtime.AimTarget = nil Runtime.AimTargetPlayer = nil Runtime.AimPredictedPosition = nil return end local camera = workspace.CurrentCamera if not camera then return end local entry = GetAimTarget() Runtime.AimTarget = entry and entry.Part or nil if not entry or not entry.Part or not entry.Part.Parent then return end local targetPosition = GetPredictedAimPosition( entry, deltaTime ) if not targetPosition then return end local cameraPosition = camera.CFrame.Position local desired = CFrame.lookAt( cameraPosition, targetPosition ) local speed = math.clamp( Settings.AimSmoothness, 0.05, 1 ) local responseRate = 10 + 26 * speed   if Settings.PredictMovingShots and IsTwistOfFateEquipped() then responseRate = math.max( responseRate, 44 ) end local alpha = 1 - math.exp( -responseRate * deltaTime ) camera.CFrame = camera.CFrame: Lerp( desired, alpha ) end ) )    Runtime:AddConnection( LocalPlayer .CharacterAdded: Connect( function() Runtime.CurrentRepair = nil Runtime.CurrentHeal = nil Runtime.CurrentExit = nil Runtime.RMBHeld = false Runtime.AimTarget = nil Runtime.AimTargetPlayer = nil Runtime.AimVelocity = Vector3.new() Runtime.AimPredictedPosition = nil Runtime.ParryReadyAt = 0 Runtime.FlaskReadyAt = 0 Runtime .GeneratorSkillSerial = Runtime .GeneratorSkillSerial + 1 Runtime .HealingSkillSerial = Runtime .HealingSkillSerial + 1 end ) )    local function StopLocalEmote() if Runtime.LocalEmoteTrack then pcall(function() Runtime.LocalEmoteTrack: Stop(0.15) Runtime.LocalEmoteTrack: Destroy() end) end Runtime.LocalEmoteTrack = nil if Runtime.LocalEmoteSound then pcall(function() Runtime.LocalEmoteSound: Stop() Runtime.LocalEmoteSound: Destroy() end) end Runtime.LocalEmoteSound = nil Runtime.CurrentEmoteName = nil end local function StopAnyEmote() pcall(function() EmoteHandler: FireServer( "StopEmote" ) end) StopLocalEmote() end local function IsEmoteEquipped( emoteName ) local ok, equipped = pcall(function() return GetEquippedEmotes: InvokeServer() end) if not ok or typeof(equipped) ~= "table" then return false end for _, name in pairs(equipped) do if typeof(name) == "string" and name == emoteName then return true end end return false end local function IsEmoteOwned( emoteName ) local ok, owned = pcall(function() return CheckEmoteOwnership: InvokeServer( emoteName ) end) return ok and owned == true end local function PlayLocalEmote( emoteName ) local folder = EmotesFolder: FindFirstChild( emoteName ) if not folder or not folder:IsA("Folder") then return false, "Emote folder missing." end local animationId = folder: GetAttribute( "animationid" ) if not animationId or tostring( animationId ) == "" then return false, "Emote animationid is missing." end local character, humanoid = GetCharacter() if not character or not humanoid or humanoid.Health <= 0 then return false, "Character is unavailable." end local animator = humanoid: FindFirstChildOfClass( "Animator" ) if not animator then animator = Instance.new( "Animator" ) animator.Parent = humanoid end StopAnyEmote() character: SetAttribute( "Sprinting", false ) local animation = Instance.new( "Animation" ) animation.AnimationId = tostring( animationId ) local ok, track = pcall(function() return animator: LoadAnimation( animation ) end) animation: Destroy() if not ok or not track then return false, "Animation failed to load." end track.Priority = Enum.AnimationPriority.Action4 track.Looped = true Runtime.LocalEmoteTrack = track Runtime.CurrentEmoteName = emoteName pcall(function() track: Play( 0.15, 1, 1 ) end) local song = folder: GetAttribute( "Song" ) if song and tostring(song) ~= "" then local sound = Instance.new( "Sound" ) sound.Name = "SaltyEmoteSound" sound.SoundId = tostring(song) sound.Volume = 0.3 sound.Looped = true sound.Parent = character: FindFirstChild( "HumanoidRootPart" ) or character Runtime.LocalEmoteSound = sound pcall(function() sound: Play() end) end Runtime: AddConnection( track.Stopped: Connect( function() if Runtime.LocalEmoteTrack == track then Runtime.LocalEmoteTrack = nil Runtime.CurrentEmoteName = nil end end ) ) return true, "local" end local function PlaySaltyEmote( emoteName ) if typeof(emoteName) ~= "string" or emoteName == "" then return false, "Invalid emote name." end StopAnyEmote()   if IsEmoteEquipped( emoteName ) then local ok = pcall(function() EmoteHandler: FireServer( emoteName ) end) if ok then Runtime.CurrentEmoteName = emoteName return true, "native" end end   return PlayLocalEmote( emoteName ) end Runtime: AddConnection( LocalPlayer .CharacterAdded: Connect( function() StopLocalEmote() end ) )    local VantaUI = loadstring( game:HttpGet( "https://raw.githubusercontent.com/MrRos3/VantaUI/main/main.lua?v=" .. tostring(os.time()) .. "-" .. tostring( math.random( 100000, 999999 ) ) ) )() Runtime.VantaUI = VantaUI pcall(function() VantaUI: SetSoundForEvent( "Notification", false ) VantaUI: SetSoundForEvent( "NotificationClose", false ) end) pcall(function() VantaUI: SetTheme( "Salty Special" ) end)    local Window = VantaUI: CreateWindow({ Title = "Salty Slasher", Icon = "skull", OpenButton = { Title = "Salty Slasher", Enabled = true, Draggable = true, OnlyMobile = false, OnlyIcon = false, CornerRadius = UDim.new( 0, 11 ), StrokeThickness = 2, Scale = 1, ImageZoom = 1, }, }) Runtime.Window = Window pcall(function() Window: OnDestroy( function() task.defer( function() if Runtime.Running then Runtime: Destroy() end end ) end ) end) pcall(function() Window: Tag({ Title = "v1.1.4.2", Icon = "badge-check", Color = Color3.fromHex( "#151116" ), Border = true, }) end)    local VisualTab = Window: Tab({ Title = "Visuals", Icon = "eye", }) local SurvivorTab = Window: Tab({ Title = "Survivor", Icon = "heart-handshake", }) local SkillTab = Window: Tab({ Title = "Skill Checks", Icon = "target", }) local MovementTab = Window: Tab({ Title = "Movement", Icon = "gauge", }) local CureTab = Window: Tab({ Title = "Cure", Icon = "flask-conical", }) local CombatTab = Window: Tab({ Title = "Combat", Icon = "crosshair", }) local EmotesTab = Window: Tab({ Title = "Emotes", Icon = "music", }) local UtilityTab = Window: Tab({ Title = "Utility", Icon = "wrench", })    VisualTab: Toggle({ Title = "Killer ESP", Desc = "Shows the tagged Killer with name and distance.", Icon = "skull", Value = Settings.KillerESP, Callback = function(value) Settings.KillerESP = value end, }) VisualTab: Toggle({ Title = "Survivor ESP", Desc = "Shows survivors, health and distance.", Icon = "users", Value = Settings.SurvivorESP, Callback = function(value) Settings.SurvivorESP = value end, }) VisualTab: Toggle({ Title = "Generator ESP", 