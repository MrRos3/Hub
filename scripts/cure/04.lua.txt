Desc = "Shows unfinished generators and repair progress.", Icon = "zap", Value = Settings.GeneratorESP, Callback = function(value) Settings.GeneratorESP = value end, }) VisualTab: Toggle({ Title = "Exit ESP", Desc = "Shows exit lever objects.", Icon = "door-open", Value = Settings.ExitESP, Callback = function(value) Settings.ExitESP = value end, }) VisualTab: Toggle({ Title = "Hook ESP", Desc = "Shows hook interaction points.", Icon = "anchor", Value = Settings.HookESP, Callback = function(value) Settings.HookESP = value end, }) VisualTab: Toggle({ Title = "Pallet ESP", Desc = "Shows usable pallet points.", Icon = "square", Value = Settings.PalletESP, Callback = function(value) Settings.PalletESP = value end, })    SurvivorTab: Toggle({ Title = "Auto Repair Nearby", Desc = "Starts repairing when an available generator point is within the game's native range.", Icon = "wrench", Value = false, Callback = function(value) Settings.AutoRepair = value if not value then StopRepair( true ) end end, }) SurvivorTab: Toggle({ Title = "Auto Heal Nearby", Desc = "Automatically heals an injured nearby survivor.", Icon = "heart-pulse", Value = false, Callback = function(value) Settings.AutoHeal = value if not value then StopHeal( true ) end end, }) SurvivorTab: Toggle({ Title = "Auto Unhook Nearby", Desc = "Automatically unhooks a survivor when an UnhookPoint is in range.", Icon = "heart-handshake", Value = false, Callback = function(value) Settings.AutoUnhook = value end, }) SurvivorTab: Toggle({ Title = "Auto Exit Nearby", Desc = "Automatically interacts with a nearby powered exit point.", Icon = "door-open", Value = false, Callback = function(value) Settings.AutoExit = value if not value then StopExit( true ) end end, }) SurvivorTab: Toggle({ Title = "Panic Pallet", Desc = "Drops a nearby pallet automatically when the Killer gets close.", Icon = "shield-alert", Value = false, Callback = function(value) Settings.PanicPallet = value end, }) SurvivorTab: Toggle({ Title = "Auto Vault / Slide", Desc = "While sprinting, uses a nearby vault or pallet-slide point.", Icon = "person-standing", Value = false, Callback = function(value) Settings.AutoVault = value end, }) SurvivorTab: Toggle({ Title = "Auto Self Unhook", Desc = "Automatically self-unhooks when allowed. By default it only fires when anti-camp or karma makes the chance 100%.", Icon = "unlock", Value = false, Callback = function(value) Settings.AutoSelfUnhook = value end, }) SurvivorTab: Toggle({ Title = "100% Self Unhook Only", Desc = "ON = Auto Self Unhook only acts when anti-camp charge or karma guarantees success.", Icon = "shield-check", Value = true, Callback = function(value) Settings.SelfUnhookGuaranteedOnly = value end, }) SurvivorTab: Button({ Title = "Take Self-Unhook Chance", Desc = "Attempts the game's native self-unhook now, even if the current chance is only 4%.", Icon = "dice-5", Callback = function() if not TrySelfUnhook( true ) then Notify( "Self Unhook", "You are not in a valid self-unhook state or it is on cooldown.", "triangle-alert" ) end end, })    SurvivorTab: Button({ Title = "Repair Nearest", Desc = "Starts the nearest available generator point if you're close enough.", Icon = "wrench", Callback = function() if not TryRepairNearest() then Notify( "Repair", "No available generator point in range.", "triangle-alert" ) end end, }) SurvivorTab: Button({ Title = "Heal Nearest", Desc = "Starts healing the nearest injured survivor.", Icon = "heart", Callback = function() if not TryHealNearest() then Notify( "Heal", "No injured survivor in range.", "triangle-alert" ) end end, }) SurvivorTab: Button({ Title = "Unhook Nearest", Desc = "Uses the nearest UnhookPoint.", Icon = "user-round-check", Callback = function() if not TryUnhookNearest() then Notify( "Unhook", "No valid unhook point in range.", "triangle-alert" ) end end, }) SurvivorTab: Button({ Title = "Use Exit", Desc = "Starts the nearest exit interaction.", Icon = "door-open", Callback = function() if not TryExitNearest() then Notify( "Exit", "No exit point in range.", "triangle-alert" ) end end, }) SurvivorTab: Button({ Title = "Drop Pallet", Desc = "Drops the nearest pallet immediately.", Icon = "square", Callback = function() if not TryDropPallet( false ) then Notify( "Pallet", "No pallet in range.", "triangle-alert" ) end end, }) SurvivorTab: Button({ Title = "Vault / Slide", Desc = "Uses the nearest window vault or pallet slide.", Icon = "move-right", Callback = function() if not TryVaultNearest() then Notify( "Vault", "No vault or slide point in range.", "triangle-alert" ) end end, })    SkillTab: Toggle({ Title = "Perfect Generator Skill Checks", Desc = "Presses during the game's exact GREAT zone rather than instantly firing the result.", Icon = "circle-dot", Value = true, Callback = function(value) Settings.PerfectGenerator = value Runtime .GeneratorSkillSerial = Runtime .GeneratorSkillSerial + 1 end, }) SkillTab: Toggle({ Title = "Perfect Healing Skill Checks", Desc = "Automatically hits the healing GREAT zone.", Icon = "heart-pulse", Value = true, Callback = function(value) Settings.PerfectHealing = value Runtime .HealingSkillSerial = Runtime .HealingSkillSerial + 1 end, }) SkillTab: Button({ Title = "Skill Check Status", Desc = "Prints current skill-check settings and input support.", Icon = "terminal", Callback = function() print("") print( "========== SALTY SKILL CHECK ==========" ) print( "Perfect Generator:", Settings.PerfectGenerator ) print( "Perfect Healing:", Settings.PerfectHealing ) print( "VirtualInputManager:", VirtualInputManager ~= nil ) print( "keypress():", type(keypress) == "function" ) print( "=======================================" ) print("") end, })    MovementTab: Toggle({ Title = "Speed Override", Desc = "Overrides normal movement speed while you aren't locked in an action.", Icon = "gauge", Value = false, Callback = function(value) Settings.SpeedOverride = value end, }) MovementTab: Button({ Title = "Speed: 17", Desc = "Native survivor sprint speed.", Icon = "rabbit", Callback = function() Settings.Speed = 17 Notify( "Speed", "17", "gauge" ) end, }) MovementTab: Button({ Title = "Speed: 22", Desc = "Moderate speed override.", Icon = "rabbit", Callback = function() Settings.Speed = 22 Notify( "Speed", "22", "gauge" ) end, }) MovementTab: Button({ Title = "Speed: 26", Desc = "Faster speed override.", Icon = "zap", Callback = function() Settings.Speed = 26 Notify( "Speed", "26", "zap" ) end, }) MovementTab: Toggle({ Title = "Auto Sprint", Desc = "Automatically keeps the native Sprinting state active while moving and not crouched or busy.", Icon = "footprints", Value = false, Callback = function(value) Settings.AutoSprint = value if not value then local character = LocalPlayer.Character if character then character: SetAttribute( "Sprinting", false ) end end end, }) MovementTab: Toggle({ Title = "Crouch", Desc = "Uses the game's crouch state and syncs Crouchingserver.", Icon = "move-down", Value = false, Callback = function(value) SetCrouch( value ) end, })    CureTab: Paragraph({ Title = "Cure Killer Suite", Desc = "Uses Cure's real replicated client rules: 5-stud infection/corpse interaction range, Cured vs HookCount validation, corpsecharge and the four active 049-2 cap.", Icon = "flask-conical", }) CureTab: Toggle({ Title = "Auto Infect Nearby", Desc = "Injects the nearest valid survivor in Cure's native interaction range.", Icon = "syringe", Value = false, Callback = function(value) Settings.AutoCureInfect = value end, }) CureTab: Toggle({ Title = "Auto Corpse Convert", Desc = "Uses Cure corpsecharge on a nearby corpse while fewer than four active 049-2 are alive.", Icon = "skull", Value = false, Callback = function(value) Settings.AutoCureCorpse = value end, }) CureTab: Button({ Title = "Inject Nearest", Desc = "Injects the nearest valid survivor now.", Icon = "syringe", Callback = function() if not TryCureInfect() then Notify( "Cure", "No valid survivor in range, wrong role, or Cure action is cooling down.", "triangle-alert" ) end end, }) CureTab: Button({ Title = "Convert Nearest Corpse", Desc = "Converts the nearest valid corpse when corpsecharge is available and the 049-2 cap allows it.", Icon = "skull", Callback = function() if not TryCureCorpse() then Notify( "Cure", "No valid corpse in range, no corpsecharge, cap reached, or action cooling down.", "triangle-alert" ) end end, }) CureTab: Button({ Title = "Prepare Flask", Desc = "Starts Cure's native flask preparation.", Icon = "flask-round", Callback = function() if not PrepareCureFlask() then Notify( "Cure Flask", "Flask is cooling down or you are not the Killer.", "triangle-alert" ) end end, }) CureTab: Button({ Title = "Throw Flask Now", Desc = "Throws using the current camera look direction and starts the native 15-second cooldown timer.", Icon = "send", Callback = function() if not ThrowCureFlask() then Notify( "Cure Flask", "Flask is cooling down or unavailable.", "triangle-alert" ) end end, }) CureTab: Button({ Title = "Cancel Flask", Desc = "Cancels a held Cure flask.", Icon = "x", Callback = function() CancelCureFlask() end, })    CombatTab: Toggle({ Title = "Hold RMB Aimbot", Desc = "Hold right mouse to smoothly lead the closest visible opponent. Shooting stays fully native; only the camera is predicted. No FOV circle.", Icon = "crosshair", Value = true, Callback = function(value) Settings.HoldRMBAimbot = value if not value then Runtime.RMBHeld = false Runtime.AimTarget = nil Runtime.AimTargetPlayer = nil Runtime.AimVelocity = Vector3.new() Runtime.AimPredictedPosition = nil end end, }) CombatTab: Slider({ Title = "Aim Smoothness", Desc = "Higher values pull the camera to the target faster while RMB is held.", Value = { Min = 0.05, Max = 1, Default = Settings.AimSmoothness, }, Step = 0.05, Callback = function(value) Settings.AimSmoothness = value end, }) CombatTab: Toggle({ Title = "Moving Target Prediction", Desc = "ON by default. Predicts a moving Killer using projectile travel time, velocity and ping, while leaving the game's Fire remote untouched.", Icon = "target", Value = true, Callback = function(value) Settings.PredictMovingShots = value Runtime.AimVelocity = Vector3.new() Runtime.AimTargetPlayer = nil end, }) CombatTab: Slider({ Title = "Prediction Strength", Desc = "1.00 is automatic. Raise slightly if fast Killers still outrun shots; lower it if bullets lead too far.", Value = { Min = 0.70, Max = 1.35, Default = Settings.PredictionStrength, }, Step = 0.01, Callback = function(value) Settings.PredictionStrength = value end, }) CombatTab: Toggle({ Title = "Auto Parry", Desc = "With Parrying Dagger equipped, auto-parries when the Killer is inside the configured danger distance and the native parry state is usable.", Icon = "shield", Value = false, Callback = function(value) Settings.AutoParry = value end, }) CombatTab: Slider({ Title = "Auto Parry Distance", Desc = "Distance in studs where Auto Parry becomes aggressive.", Value = { Min = 3, Max = 12, Default = Settings.ParryDistance, }, Step = 0.5, Callback = function(value) Settings.ParryDistance = value end, })    EmotesTab: Button({ Title = "Stop Emote", Desc = "Stops both native and Salty local emotes.", Icon = "square", Callback = function() StopAnyEmote() end, }) local emoteNames = {} for _, emoteObject in ipairs( EmotesFolder: GetChildren() ) do if emoteObject:IsA("Folder") and emoteObject: GetAttribute( "animationid" ) then table.insert( emoteNames, emoteObject.Name ) end end table.sort( emoteNames, function(a, b) return string.lower(a) < string.lower(b) end ) local ownedCount = 0 local equippedCount = 0 pcall(function() for _, name in ipairs(emoteNames) do if IsEmoteOwned(name) then ownedCount = ownedCount + 1 end if IsEmoteEquipped(name) then equippedCount = equippedCount + 1 end end end) EmotesTab: Paragraph({ Title = "All Game Emotes", Desc = "Loaded " .. tostring(#emoteNames) .. " replicated emotes. " .. tostring(equippedCount) .. " equipped emotes use the game's native server path; every other button falls back to the replicated animation directly.", Icon = "music", }) for _, emoteName in ipairs(emoteNames) do local name = emoteName EmotesTab: Button({ Title = name, Desc = "Play " .. name, Icon = "play", Callback = function() local ok, mode = PlaySaltyEmote( name ) if not ok then Notify( "Emote", tostring(mode), "triangle-alert" ) end end, }) end    UtilityTab: Button({ Title = "Stop Current Action", Desc = "Stops repair, healing and exit interactions cleanly.", Icon = "square", Callback = function() StopRepair( true ) StopHeal( true ) StopExit( true ) SetActionAttribute( "isUnhooking", false ) SetActionAttribute( "isDroppingPallet", false ) SetActionAttribute( "isVaulting", false ) SetActionAttribute( "isSliding", false ) Notify( "Actions", "Stopped.", "square" ) end, }) UtilityTab: Button({ Title = "Print Status", Desc = "Shows your role, action state and nearby targets.", Icon = "terminal", Callback = function() local character, humanoid, root, check = GetCharacter() local killer, killerDistance = GetNearestKiller() local generator, generatorDistance = FindNearestTagged( "GeneratorPoint", math.huge ) print("") print( "========== SALTY SLASHER ==========" ) print( "PlaceId:", game.PlaceId ) print( "Team:", LocalPlayer.Team and LocalPlayer.Team.Name or "None" ) print( "Health:", humanoid and humanoid.Health or "?" ) print( "Busy:", IsBusy() ) print( "Repairing:", check and check: GetAttribute( "isRepairing" ) ) print( "Healing:", check and check: GetAttribute( "isHealing" ) ) print( "Current Repair:", Runtime.CurrentRepair ) print( "Current Heal:", Runtime.CurrentHeal ) print( "Nearest Killer:", killer and killer.Name or "None", killerDistance ) print( "Nearest Generator Point:", generator, generatorDistance ) print( "Root:", root ) print( "====================================" ) print("") end, })    function Runtime:Destroy() if not self.Running then return end  self.RMBHeld = false self.AimTarget = nil self.AimTargetPlayer = nil self.AimVelocity = Vector3.new() self.AimPredictedPosition = nil pcall(function() SetCrouch( false ) end) pcall(function() local character = LocalPlayer.Character if character then character: SetAttribute( "Sprinting", false ) end end) pcall(function() StopAnyEmote() end) pcall(function() CancelCureFlask() end) pcall(function() StopRepair( true ) end) pcall(function() StopHeal( true ) end) pcall(function() StopExit( true ) end) self.Running = false self.GeneratorSkillSerial = self.GeneratorSkillSerial + 1 self.HealingSkillSerial = self.HealingSkillSerial + 1 for _, connection in ipairs( self.Connections ) do pcall(function() connection: Disconnect() end) end table.clear( self.Connections ) local objects = {} for object in pairs( self.ESPRecords ) do table.insert( objects, object ) end for _, object in ipairs(objects) do DestroyESPRecord( object ) end if self.ESPFolder then pcall(function() self.ESPFolder: Destroy() end) end if ENV.__SALTY_SLASHER == self then ENV.__SALTY_SLASHER = nil end if self.Window then pcall(function() if not self.Window.Destroyed then self.Window: Destroy() end end) end print( "[Salty Slasher] stopped." ) end UtilityTab: Button({ Title = "Stop Salty Slasher", Desc = "Stops the entire script and cleans ESP.", Icon = "power", Callback = function() Runtime: Destroy() end, })    Notify( "Salty Slasher v1.0", "Loaded • native shooting restored + predictive RMB aim ready.", "skull" ) print("") print( "========================================" ) print( "SALTY SLASHER v1.1.4.2" ) print( "PlaceId:", game.PlaceId ) print( "Native Detection Radius:", Settings.DetectionRadius ) print( "Perfect Generator:", Settings.PerfectGenerator ) print( "Perfect Healing:", Settings.PerfectHealing ) print( "========================================" ) print("")